package main

import (
	"encoding/json"
	"fmt"
)

type A struct {
	A int      `json:"a,omitempty"`
	B string   `json:"b,omitempty"`
	C []string `json:"c,omitempty"`
}

func main() {
	a := A{A: 0}
	b, _ := json.Marshal(a)
	fmt.Println(string(b))
}
